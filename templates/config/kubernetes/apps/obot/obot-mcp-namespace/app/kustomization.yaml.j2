#% if obot_enabled | default(false) %#
---
# NO namespace: field here - this prevents kustomize from renaming the Namespace resource
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
#% endif %#
