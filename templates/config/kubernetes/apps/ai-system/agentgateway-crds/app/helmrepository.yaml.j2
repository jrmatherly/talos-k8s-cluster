#% if agentgateway_enabled | default(true) %#
---
#| ============================================================ #|
#| HelmRepository for agentgateway charts (MCP/AI control plane) #|
#| Uses cr.agentgateway.dev registry (dedicated agentgateway)    #|
#| See: docs/guides/agentgateway-migration.md                    #|
#| ============================================================ #|
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: agentgateway
spec:
  interval: 1h
  type: oci
  url: oci://cr.agentgateway.dev/charts
#% endif %#
