#% if litellm_enabled %#
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ocirepository.yaml
  - helmrelease.yaml
  - configmap.yaml
  - secret.sops.yaml
  - db-secret.sops.yaml
  - dragonfly.yaml
  - postgresql.yaml
  - networkpolicy.yaml
  #| ReferenceGrant allows network/litellm-route to access this service #|
  - referencegrant.yaml
  #| NOTE: HTTPRoute and TrafficPolicy removed - using network/litellm-route instead #|
  #| llms.${SECRET_DOMAIN} is the preferred endpoint for LiteLLM access #|
#% if observability_enabled %#
  - servicemonitor.yaml
#% endif %#
#% endif %#
