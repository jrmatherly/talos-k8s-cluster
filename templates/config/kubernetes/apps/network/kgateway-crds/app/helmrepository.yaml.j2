#% if kgateway_enabled | default(true) %#
---
#| ============================================================ #|
#| HelmRepository for kgateway charts (Envoy control plane)      #|
#| Uses cr.kgateway.dev registry                                 #|
#| REF: https://kgateway.dev/docs/agentgateway/main/quickstart/  #|
#| ============================================================ #|
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: kgateway
spec:
  interval: 1h
  type: oci
  url: oci://cr.kgateway.dev/kgateway-dev/charts
#% endif %#
