#| Performance tuning for consistent CPU performance #|
#| REF: https://docs.siderolabs.com/talos/v1.11/configure-your-talos-cluster/system-configuration/performance-tuning #|
#| NOTE: Requires node upgrade (not just config apply) to take effect #|
machine:
  install:
    extraKernelArgs:
      #| CPU scaling governor - 'performance' ensures maximum CPU frequency #|
      #| Best for: AI workloads (LiteLLM, Cognee), latency-sensitive services #|
      #| Trade-off: Higher power consumption vs consistent performance #|
      - cpufreq.default_governor=performance
