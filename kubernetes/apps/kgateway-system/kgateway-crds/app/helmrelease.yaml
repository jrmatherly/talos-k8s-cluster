---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kgateway-crds
spec:
  chartRef:
    kind: OCIRepository
    name: kgateway-crds
  interval: 1h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
