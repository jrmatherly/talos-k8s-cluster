---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: azure-openai-eastus2-responses
spec:
  parentRefs:
    - name: agentgateway
      namespace: kgateway-system
  hostnames:
    - "llms.matherly.net"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /azure/eastus2/responses
      backendRefs:
        - group: gateway.kgateway.dev
          kind: Backend
          name: azure-openai-eastus2-responses
