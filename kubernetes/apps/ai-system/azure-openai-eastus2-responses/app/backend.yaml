---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: azure-openai-eastus2-responses
  labels:
    cost-center: ai-gateway
    provider: azure-openai
    region: eastus2
    model-type: responses
    app.kubernetes.io/name: azure-openai-eastus2-responses
    app.kubernetes.io/component: ai-backend
    app.kubernetes.io/part-of: ai-system
spec:
  ai:
    provider:
      azureopenai:
        endpoint: "ets-east-us2.openai.azure.com"
        deploymentName: "gpt-5.1"
        apiVersion: "2025-04-01-preview"
  policies:
    auth:
      secretRef:
        name: azure-openai-eastus2-responses-credentials
