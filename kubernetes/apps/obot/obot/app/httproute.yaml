---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: obot-ui
spec:
  hostnames:
    - "obot.${SECRET_DOMAIN}"
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: obot
          port: 8080
