---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: agentgateway-crds
spec:
  chart:
    spec:
      chart: agentgateway-crds
      version: "v2.2.0-main"
      sourceRef:
        kind: HelmRepository
        name: agentgateway
  interval: 1h
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
