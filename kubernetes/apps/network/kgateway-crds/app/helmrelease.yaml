---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kgateway-crds
spec:
  chart:
    spec:
      chart: kgateway-crds
      version: "v2.2.0-beta.3"
      sourceRef:
        kind: HelmRepository
        name: kgateway
  interval: 1h
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
